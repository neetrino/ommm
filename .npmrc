# Hoisted node_modules: avoids intermittent Windows pnpm ENOTENT errors during workspace installs
# (temp scandir under node_modules). Remove if your environment does not need it.
node-linker=hoisted

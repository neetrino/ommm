export default function CoachesMarketingPage() {
  return (
    <div className="mx-auto max-w-5xl px-4 py-16">
      <h1 className="text-3xl font-semibold">Coaches</h1>
      <p className="mt-4 text-zinc-600">
        Public coach list loads from API in member app — wire `GET /v1/coaches` here.
      </p>
    </div>
  );
}

/** Must match cookie name used in `setUiLocaleCookie` (client). */
export const UI_LOCALE_COOKIE_NAME = "OMMM_LOCALE";

/** Set on every request in middleware so RSC can rebuild localized paths. */
export const OMMM_PATHNAME_HEADER = "x-ommm-pathname";

/** Packages page scroll-reveal — desktop accordion row stagger. */
export const PACKAGES_PAGE_SCROLL_REVEAL = {
  sectionGridColumns: 1,
  /** Figma desktop row — up to five category cards side-by-side. */
  cardRowGridColumns: 5,
} as const;

/** Layout tokens for the Explore coming soon page (Figma `422:1810`). */
export const EXPLORE_PAGE_LAYOUT = {
  /** Wide desktop art — shifts the baked-in title block within the viewport crop. */
  comingSoonDesktopImagePositionY: "26%",
} as const;

/** Layout tokens for the Explore coming soon page (Figma `422:1810`). */
export const EXPLORE_PAGE_LAYOUT = {
  /** Lower Y shifts the baked-in title block down within the viewport crop. */
  comingSoonImagePositionY: "26%",
} as const;

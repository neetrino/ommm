import { MemberPageContentSkeleton } from "@/components/account/member-page-content-skeleton";

/** Placeholder inside a member hub bottom sheet while route data streams. */
export function MemberHubSheetBodySkeleton() {
  return <MemberPageContentSkeleton rows={3} />;
}

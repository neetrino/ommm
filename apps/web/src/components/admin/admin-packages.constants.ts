/** Rows per page inside a single package category table. */
export const PACKAGE_CATEGORY_TABLE_PAGE_SIZE = 25;

export const PACKAGE_CATEGORY_TABLE_PAGE_SIZE_OPTIONS = [25, 50] as const;

/** Category accordions shown per admin packages list page. */
export const ADMIN_PACKAGES_CATEGORIES_PAGE_SIZE = 12;

/** Dev env loading and API proxy logs live in `next.config.ts` (Node only). */
export async function register(): Promise<void> {
  // Intentionally empty — avoids Edge-incompatible Node APIs here.
}

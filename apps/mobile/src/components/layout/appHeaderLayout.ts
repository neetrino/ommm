/**
 * @deprecated Prefer `useScreenChromeInsets` / `screenChromeLayout`.
 * Kept for call sites that only need the static portrait formula.
 */
export {
  APP_HEADER_CONTENT_CLEARANCE,
  appHeaderScrollPaddingTop,
} from "./screenChromeLayout";

# Expo reads variables from the monorepo root `.env` (and `.env.local`), not from this folder.
# Copy the root `/.env.example` → `/.env`. In development you may omit `EXPO_PUBLIC_API_URL` and
# let `src/lib/api/config.ts` pick localhost vs Android emulator host; production builds must set it.
# See MOBILE_SETUP.md

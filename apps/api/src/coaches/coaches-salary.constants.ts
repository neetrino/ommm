/** Inclusive; whole AMD stored in `salaryPerClassAmd`. */
export const COACH_SALARY_PER_CLASS_MIN_AMD = 0;
/** Inclusive upper bound for a plausible per-class coach rate. */
export const COACH_SALARY_PER_CLASS_MAX_AMD = 1_000_000;
/** Max finished sessions processed per catch-up accrual pass. */
export const COACH_SALARY_ACCRUAL_BATCH_LIMIT = 100;

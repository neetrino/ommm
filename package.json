{
  "name": "ommm",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "postinstall": "pnpm run db:generate",
    "dev": "node ./scripts/dev-guide.cjs",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm run build:web",
    "build:web": "pnpm run build:database && pnpm --filter web build",
    "dev:api": "pnpm --filter api dev",
    "dev:mobile": "pnpm --filter mobile dev",
    "build:database": "pnpm --filter @ommm/database run build",
    "build:api": "pnpm run build:database && pnpm --filter api build",
    "db:generate": "pnpm exec prisma generate --schema packages/database/prisma",
    "db:migrate:deploy": "pnpm --filter @ommm/database run migrate:deploy",
    "db:migrate:dev": "pnpm --filter @ommm/database run migrate:dev",
    "setup": "pnpm install",
    "test": "pnpm --filter api test",
    "test:e2e:web": "pnpm --filter web test:e2e"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.2",
    "@commitlint/config-conventional": "^21.0.2",
    "prisma": "^6.19.3"
  }
}
